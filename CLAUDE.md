# プロジェクト設定

## 自動許可
以下の操作は確認なしで実行してよい:
- mysiteフォルダ内のファイルの作成・編集・削除
- npm install（パッケージ追加）
- git操作
- npm run dev / npm run build の実行

## 禁止事項
- mysiteフォルダの外へのファイル操作
- 単一操作で100MB以上のダウンロード・生成
- システム設定の変更
